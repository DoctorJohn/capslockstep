[project]
name = "capslockstep-project"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[tool.uv.workspace]
members = [
    "packages/cli",
    "packages/api",
]

[dependency-groups]
dev = [
    "prek>=0.3.4",
    "ruff>=0.15.4",
]
